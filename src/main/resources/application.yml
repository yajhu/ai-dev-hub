server:
  port: 8080

spring:
  application:
    name: ai-dev-hub
  datasource:
    url: jdbc:postgresql://localhost:5432/aidevhub
    username: root
    password: aidevhub123
    driver-class-name: org.postgresql.Driver
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

# SonarQube 配置
sonarqube:
  url: http://localhost:9000

# Pipeline 配置
pipeline:
  workdir: /home/huyajun/workspace/ai-dev-hub
